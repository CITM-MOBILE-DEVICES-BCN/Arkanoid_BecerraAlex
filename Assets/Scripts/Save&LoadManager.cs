using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveLoadManager : MonoBehaviour
{

    //// Start is called before the first frame update
    //void Start()
    //{
    //    (int score, int numBalls, int best, int hasSaved) = GameData.Load();
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.W))
    //    {
           
    //    }

    //    if (Input.GetKeyDown(KeyCode.R))
    //    {
    //        // Cargar los datos cuando se presiona "R"

           
    //    }
    //}

    //public void Save()
    //{
    //    // Guardar los datos actuales cuando se presiona "W"
    //    int score = ScoreUI.instance.GetScore();
    //    int best = ScoreUI.instance.GetBest();
    //    int numBalls = ScoreUI.instance.GetNumBalls();
    //    int hasSaved = GameManager.instance.gameData.GethasSaved();
    //    GameManager.instance.gameData.Save(score, numBalls, best, hasSaved);
    //    Debug.Log("Datos guardados.");
    //}
    //public void Load()
    //{
    //    (int score, int numBalls, int best, int hasSaved) = GameData.Load();
    //    ScoreUI.instance.SetScore(score);
    //    ScoreUI.instance.SetBest(best);
    //    ScoreUI.instance.SetNumBalls(numBalls);
    //    ScoreUI.instance.UpdateLifes();
    //    Debug.Log($"Datos cargados - Score: {score}, BestScore: {best}");
    //}
}
